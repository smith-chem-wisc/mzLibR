% Generated by scripts/build-man.R from the roxygen-style block in the source.
% Do not edit by hand; edit R/ and re-run.
\name{pride_locations}
\alias{pride_locations}
\title{The published locations of each file, as controlled-vocabulary terms}
\usage{
pride_locations(files)
}
\arguments{
\item{files}{A \code{\link{pride_list_files}} data.frame.}

}
\description{
One long data.frame rather than a nested list, so it pipes into anything.
}
\details{
You rarely need this: \code{https_url} already carries the fetchable URL, resolved by mzLib's
\code{TryGetHttpsDownloadUrl}, which \strong{searches} the locations rather than taking the first.
That distinction matters - \strong{the order is not stable.} In PXD000001 the mztab lists FTP
first while the MGF lists \strong{Aspera} first, so code that took \code{locations[[1]]} would get an
unfetchable \code{prd_ascp@fasp.ebi.ac.uk:...} address for some files and a working one for
others, in the same project. Do not re-implement the search.
}
\value{
A data.frame with \code{file_name}, \code{accession}, \code{name} and \code{value}, one row per location.
}
