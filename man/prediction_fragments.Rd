% Generated by scripts/build-man.R from the roxygen-style block in the source.
% Do not edit by hand; edit R/ and re-run.
\name{prediction_fragments}
\alias{prediction_fragments}
\title{Predict MS2 fragment m/z and relative intensity}
\usage{
prediction_fragments(model, peptides, out = NULL, max_batches = NULL,
  throttle_ms = NULL, timeout = NULL)
}
\arguments{
\item{model}{A fragment-intensity model name, e.g. \code{"Prosit_2020_intensity_HCD"}.}

\item{peptides}{A character vector of sequences, or a data.frame whose columns may include
  \code{sequence}, \code{precursor_charge}, \code{collision_energy}, \code{instrument_type} and
  \code{fragmentation_type}. Check \code{\link{prediction_models}} for which this model requires.}

\item{out}{Write a tab-separated table here; the arrays become \code{;}-joined lists.}

\item{max_batches}{In-flight requests.}

\item{throttle_ms}{Delay between request chunks.}

\item{timeout}{Seconds to allow, or \code{NULL} to wait indefinitely.
}

}
\description{
Predict MS2 fragment m/z and relative intensity
}
\value{
An \code{mzlibr_predictions} whose \code{fragment_annotations}, \code{fragment_mz} and
  \code{fragment_intensity} columns are \strong{list columns}, one array per row.

}
\section{The arrays are ragged, and that is the correct answer}{


Koina returns a fixed-width grid with \code{-1} marking ions that cannot exist for a given peptide,
and mzLib drops those. So each row's three arrays are as long as \strong{that peptide's} possible
ions - 28 for a short tryptic peptide from a model whose nominal count is 174 - and two rows in
one call will differ. They are index-aligned within a row, and are a list column precisely
because they cannot be a matrix.

Intensities are \strong{relative}, on Koina's own 0-1 scale, and are not comparable with a measured
intensity or between models.

}
\seealso{
\code{\link{prediction_models}}
}
