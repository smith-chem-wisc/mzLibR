% Generated by scripts/build-man.R from the roxygen-style block in the source.
% Do not edit by hand; edit R/ and re-run.
\name{sdrf_records}
\alias{sdrf_records}
\title{An SDRF document as a data.frame, when its shape allows one}
\usage{
sdrf_records(doc)
}
\arguments{
\item{doc}{An \code{\link{sdrf_read}} or \code{\link{sdrf_pool}} result.
}

}
\description{
The convenient shape, and a \strong{lossy} one when a column name repeats: each name becomes one
column holding its \strong{last} occurrence a row reaches, which is what pyMzLib's \code{records} does.
\code{\link{sdrf_has_repeated_columns}} says whether that applies. A position a row is too short to
reach is \code{NA}.
}
\value{
A data.frame with one row per returned row and one column per distinct name, in order
  of first appearance. Names are kept verbatim (\code{check.names = FALSE}).
}
