% Generated by scripts/build-man.R from the roxygen-style block in the source.
% Do not edit by hand; edit R/ and re-run.
\name{prediction_ccs}
\alias{prediction_ccs}
\title{Predict collisional cross-section, in square angstroms}
\usage{
prediction_ccs(model, peptides, out = NULL, max_batches = NULL, throttle_ms = NULL,
  timeout = NULL)
}
\arguments{
\item{model}{A CCS model name, e.g. \code{"IM2Deep"}.}

\item{peptides}{A data.frame with \code{sequence} and \code{precursor_charge} columns. \strong{CCS depends on
  charge}, so a bare character vector is rarely what you want.}

\item{out}{Write a tab-separated table here.}

\item{max_batches}{In-flight requests.}

\item{throttle_ms}{Delay between request chunks.}

\item{timeout}{Seconds to allow, or \code{NULL} to wait indefinitely.
}

}
\description{
Predict collisional cross-section, in square angstroms
}
\value{
An \code{mzlibr_predictions}. The unit is \strong{square angstroms, never 1/K0} - converting to
  the reduced mobility a timsTOF reports needs drift-gas temperature and pressure, which mzLib
  does not carry, so no conversion is offered rather than a guessed one.

}
\seealso{
\code{\link{prediction_models}}
}
