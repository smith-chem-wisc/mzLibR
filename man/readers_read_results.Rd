% Generated by scripts/build-man.R from the roxygen-style block in the source.
% Do not edit by hand; edit R/ and re-run.
\name{readers_read_results}
\alias{readers_read_results}
\title{Read a result file into the uniform record view}
\usage{
readers_read_results(path, limit = NULL, offset = 0, out = NULL, timeout = NULL)
}
\arguments{
\item{path}{Path to a MetaMorpheus \code{.psmtsv} / \code{.osmtsv} or an MSFragger \code{psm.tsv}.}

\item{limit}{Maximum records to return. \code{NULL}, the default, returns all of them.

  \strong{There is no default row limit}, deliberately. A result file can carry a million rows, and
  truncating by default would mean the ordinary call returns a table that looks complete and
  is not. \code{truncated} reports whether anything was left behind.}

\item{offset}{Records to skip.

  \strong{This is a window, not a cursor.} mzLib materialises the whole file on every call — its
  readers look lazy and are not — so paging re-reads and re-parses the file once per page. A
  loop over pages is quadratic. For a large file use \code{out} instead, in one call.}

\item{out}{Write the records to this path as a \strong{tab-separated} table and return only a
  summary, instead of carrying them back in the envelope. The intended path for large files.

  Tab-separated, not comma-separated, because these fields contain commas: MSFragger's mapped
  proteins are a comma-separated list inside a single field. Read it with
  \code{read.delim(path)}, not \code{read.csv(path)}.}

\item{timeout}{Seconds to allow, or \code{NULL} to wait indefinitely. A large file legitimately
  takes a while.
}

}
\description{
Read a result file through the cross-format quantifiable view: sequence, retention time, charge, theoretical mass, decoy flag and protein groups, the same columns for every format that offers it.

Only the three file types offering the \code{"quantifiable"} view can be read this way — check
\code{\link{readers_identify}} first, or catch the error, which names the views the file does have.
}
\value{
An \code{mzlibr_result_records}, carrying the file's \code{reader}, \code{absent_fields},
  \code{failed_fields} and \code{excluded_fields} as every read does. \code{record_count} counts the records in
  the \strong{whole file}
  regardless of \code{limit} and \code{offset}; \code{returned_count} counts the records that came back,
  starting \code{offset} records in; \code{rows_not_read} counts data rows that did not become records.

  \code{records} is a data.frame of the record view, one row per record, or \code{NULL} when \code{out} was
  given. \code{retention_time} is in minutes for all four formats at mzLib 1.0.592, and
  \code{retention_time_unit} says so per file; \code{charge_state} is a charge; \code{monoisotopic_mass} is a
  neutral mass in Da. \code{is_decoy} is \code{NA} where the format records no decoy label.

}
\section{Two fields to read before you trust the table}{


\code{rows_not_read} — data rows that did not become records. \strong{mzLib drops a malformed row
silently}, so a non-zero value here means the file is partly unreadable and your table is
incomplete. \code{NA} when the count could not be established.

\code{caveats} — what the uniform view cannot be trusted to mean for this format, each citing the
mzLib source it came from. Empty for some formats and not for others. This is where you learn
that, e.g., TopPIC retention times are seconds while MetaMorpheus's and MSFragger's are minutes.

Relatedly, \code{retention_time_unit} is per format and mzLib does \strong{not} normalise it. Convert
with \code{\link{readers_retention_time_in_minutes}} rather than by hand.

}
\section{Wraps}{
Wire verb \code{readers read-results}. Generated from the bridge's verb spec \code{readers.read-results.yaml} (bridge commit \code{5db922d4cfe1}) by \code{scripts/build-man.R}; the spec owns these facts, and all three bindings render the same ones.

\itemize{
\item \href{https://github.com/smith-chem-wisc/mzLib/blob/23c2490e/mzLib/Readers/FileReader.cs}{\code{FileReader.ReadQuantifiableResultFile}} in \code{mzLib/Readers/FileReader.cs} at mzLib \code{23c2490e}
\item \href{https://github.com/smith-chem-wisc/mzLib/blob/23c2490e/mzLib/Readers/BaseClasses/IQuantifiableRecord.cs}{\code{IQuantifiableRecord}} in \code{mzLib/Readers/BaseClasses/IQuantifiableRecord.cs} at mzLib \code{23c2490e}
}
}
\section{Parameters: units, ranges and defaults}{
\describe{
\item{\code{path}}{path; required. A MetaMorpheus .psmtsv/.osmtsv, an MSFragger psm.tsv or a DIA-NN report.tsv.}
\item{\code{offset}}{int; in \strong{records}; default \code{0}; range \code{>= 0}. Skip this many records. A window, not a cursor: mzLib parses the whole file on every call, so paging re-reads it.}
\item{\code{limit}}{int; in \strong{records}; default absent; range \code{>= 0}. Return at most this many records. Absent means no limit; there is deliberately no default cap.}
\item{\code{out}}{path; default absent. Write the table to this path as tab-separated text instead of returning columns. Must differ from path. Parent directories are created.}
}
}
\section{Returned fields}{
Each field with its type, its unit, and what \code{NA} means when it is \code{NA}.

\describe{
\item{\code{path}}{string; never \code{NA}. Absolute path of the input.}
\item{\code{file_type}}{string; never \code{NA}. The mzLib SupportedFileType that was dispatched.}
\item{\code{reader}}{string; never \code{NA}. The mzLib reader class that parsed the file.}
\item{\code{record_count}}{int; in \strong{records}; never \code{NA}. Records in the whole file, before the window.}
\item{\code{rows_not_read}}{int; in \strong{rows}; \code{NA} when the count is not meaningful for this format (only the psmtsv family and MSFragger are one line per record). Data rows that did not become records: mzLib drops a malformed line silently.}
\item{\code{retention_time_unit}}{string; never \code{NA}. The unit of retention_time for THIS format: minutes for all four at mzLib 1.0.592, unknown when mzLib gives no basis.}
\item{\code{caveats}}{string[]; never \code{NA}. What this view cannot be trusted to mean for THIS file, each citing the mzLib source.}
\item{\code{column_names}}{string[]; never \code{NA}. Column order.}
\item{\code{absent_fields}}{string[]; never \code{NA}. Columns the view defines that this file's format has no column or source for; null in every row (BULK.md section 4). Empty when every column has a source.}
\item{\code{failed_fields}}{string[]; never \code{NA}. 'field: ExceptionType' for each column whose read threw on some returned rows; those cells are null.}
\item{\code{excluded_fields}}{object[]; never \code{NA}. \{field, type, reason, verb\} for each field with no column shape; verb names the command that carries it, or null.}
\item{\code{error}}{object; \code{NA} when always, for a single path: a file that cannot be read fails the call instead. \{kind, type, message\}; non-null only in a files[] entry under on-error skip.}
\item{\code{returned_count}}{int; in \strong{records}; never \code{NA}. Records returned in columns (the unit offset and limit count in); 0 when written to out.}
\item{\code{offset}}{int; in \strong{records}; never \code{NA}. The offset applied, in records.}
\item{\code{truncated}}{bool; never \code{NA}. True whenever records were left out by offset or limit.}
\item{\code{records} (wire \code{columns})}{table; \code{NA} when the table was written to out instead. Column name to per-row values, in column_names order.}
\item{\code{output}}{object; \code{NA} when out was not given. \{path, format: "tsv", row_count\} when out was given.}
}
}
\section{Columns of \code{records}}{
\describe{
\item{\code{file_name}}{string; never \code{NA}. The spectra file the record came from, as the format writes it (not a join key across formats).}
\item{\code{base_sequence}}{string; never \code{NA}. Unmodified sequence.}
\item{\code{full_sequence}}{string; never \code{NA}. Modified sequence; on an ambiguous psmtsv row every candidate '|'-joined.}
\item{\code{retention_time}}{float; in \strong{min}; \code{NA} when mzLib's -1 'absent' sentinel, or a non-finite value. Retention time, in retention_time_unit.}
\item{\code{charge_state}}{int; in \strong{charge}; never \code{NA}. Precursor charge.}
\item{\code{monoisotopic_mass}}{float; in \strong{Da}; \code{NA} when mzLib's -1 'absent' sentinel. THEORETICAL peptide mass, not the observed precursor.}
\item{\code{is_decoy}}{bool; \code{NA} when the format cannot report decoys: MSFragger and DIA-NN, where is_decoy is also in absent_fields. Decoy flag where the format carries one.}
\item{\code{protein_accessions}}{string; never \code{NA}. ';'-joined accessions.}
\item{\code{gene_name}}{string; never \code{NA}. ';'-joined gene names.}
\item{\code{organism}}{string; never \code{NA}. ';'-joined organisms.}
}
}
\section{Errors}{
Each is an R condition carrying the class shown and \code{mzlib_error}; see \code{\link{mzlib_error}}. A condition that mentions \code{paths-stdin}, \code{threads} or \code{on-error} comes only from \code{\link{readers_read_results_many}()}.

\describe{
\item{\code{mzlib_usage_error} (usage)}{path missing or blank; file or directory not found; mzLib does not recognise the file type (the message points at readers formats)}
\item{\code{mzlib_usage_error} (usage)}{offset, limit or out given with no value; offset or limit < 0; non-integer value; out resolves to the same file as path}
\item{\code{mzlib_usage_error} (usage)}{the file has no quantifiable view (the message names the views it does have)}
\item{\code{mzlib_bridge_error} (correctness)}{mzLib recognises the file and fails to parse it}
\item{\code{mzlib_usage_error} (usage)}{paths-stdin with path, offset or limit; paths-stdin with a value; no paths on stdin; a repeated path; threads 0 or below -1; on-error not fail or skip; on-error skip with a single path; out naming an input}
\item{\code{mzlib_usage_error} (usage)}{with on-error fail, an input that is missing or not this view: the usage error of that input, prefixed 'Input <i> (<path>)'}
\item{\code{mzlib_bridge_error} (correctness)}{with on-error fail, mzLib failing to parse an input: that input's own error type, message prefixed 'Input <i> (<path>)'}
\item{\code{mzlib_service_unavailable} (service_unavailable)}{Never raised by this verb.}
}
}
\section{Caveats}{
\itemize{
\item No q-value, PEP or score: nothing here is FDR-filtered.
\item is_decoy is absent for MSFragger and DIA-NN, which write no target/decoy label; null there means unknown, not target.
\item monoisotopic_mass is the theoretical mass in every format; the observed precursor is not in this view.
\item psmtsv: monoisotopic_mass keeps the first candidate of an ambiguous row while full_sequence keeps them all (SpectrumMatchFromTsv.cs:119, :194).
\item A malformed psmtsv row is dropped silently by mzLib; rows_not_read counts them.
\item Many files: \code{--paths-stdin} reads the list in ONE process, \code{--threads} at a time, into one long table with source_index and source_path first. The output does not depend on \code{--threads} (a tested property); the default of 1 is a memory choice, since every reader materialises the whole file.
}
}
\section{Performance}{
Every call starts one bridge process, which costs a .NET start-up before any work. For many files call \code{\link{readers_read_results_many}()} once rather than looping this function: one process, one start-up, and the thread count stated on the wire.
}
\section{Same verb in other bindings}{
\itemize{
\item Python (pyMzLib): \code{pymzlib.readers.read_results}; many files: \code{pymzlib.readers.read_results_many}
\item Rust (mzLibRust): \code{mzlib::readers::read_results_with} with \code{ReadOptions}; many files: \code{mzlib::readers::read_results_many}
\item R (mzLibR): \code{readers_read_results}; many files: \code{\link{readers_read_results_many}}
}
}
\section{Since}{
Wire protocol 1; pyMzLib 0.1.0; mzLibRust 0.1.0; mzLibR 0.1.0.
}
\section{Not yet verified}{
The spec records these as open. They are listed rather than hidden:

\itemize{
\item The _many spellings (py, rust, r) follow the 2026-09-23 cross-binding decision; Rust and R names here are intended, not yet ported, and since.pymzlib stays null until release.
}
}
\seealso{
\code{\link{readers_read_results_many}}, \code{\link{readers_identify}},
  \code{\link{readers_retention_time_in_minutes}}
}
\examples{

\dontshow{.mzlibr_example <- mzLibR:::replay_bridge_start()}
psms <- readers_read_results("FraggerPsm_FragPipev21.1_psm.tsv", limit = 2)
psms
psms$records[, c("base_sequence", "charge_state", "retention_time")]
readers_retention_time_in_minutes(psms)
\dontshow{mzLibR:::replay_bridge_stop(.mzlibr_example)}
}
