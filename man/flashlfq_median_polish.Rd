% Generated by scripts/build-man.R from the roxygen-style block in the source.
% Do not edit by hand; edit R/ and re-run.
\name{flashlfq_median_polish}
\alias{flashlfq_median_polish}
\title{Roll a FlashLFQ peptide table up to protein intensities, under a new design}
\usage{
flashlfq_median_polish(peptides, design = NULL, use_shared_peptides = FALSE,
  output_directory = NULL, timeout = NULL)
}
\arguments{
\item{peptides}{Path to a FlashLFQ \code{QuantifiedPeptides.tsv} - the file \code{\link{flashlfq_quantify}}
  writes into \code{output_directory}. It must carry \code{Sequence}, \code{Base Sequence}, \code{Protein Groups}
  and one \code{Intensity_<run>} column per run; a blank intensity cell is read as 0 (not measured).}

\item{design}{The experimental design: a data.frame with one row per run, a \code{file_name} column
  matching each \code{Intensity_<file_name>} column, and optional \code{condition},
  \code{biological_replicate}, \code{technical_replicate} and \code{fraction} columns (0-based whole
  numbers); or a character vector of run names. \strong{Median polish groups runs by condition and
  biological replicate, so this is how you say which runs are replicates of which sample} -
  the whole reason to re-run it. A design must name every run in the table and only those.
  \code{NULL}, the default, makes each run its own biological replicate with a blank condition,
  which is what FlashLFQ assumes when it writes the file with no design.}

\item{use_shared_peptides}{Whether peptides shared between protein groups contribute. \code{FALSE}
  by default, when a group with only shared peptides quantifies to 0.}

\item{output_directory}{Where FlashLFQ should also write \code{QuantifiedProteins.tsv}, or \code{NULL} to
  write nothing. Its columns are labelled by the same rule as \code{samples$label}.}

\item{timeout}{Seconds to allow, or \code{NULL} to wait as long as it takes.
}

}
\description{
Roll a FlashLFQ QuantifiedPeptides.tsv up to protein intensities per sample with FlashLFQ's median polish, under a new experimental design, without re-reading any spectra.

The second half of \code{\link{flashlfq_quantify}} on its own. Given a \code{QuantifiedPeptides.tsv} FlashLFQ
already wrote, it rebuilds FlashLFQ's peptide and protein graph and runs the same median-polish
protein quantification, without re-reading any spectra. Reach for it to re-quantify proteins
under a different experimental design, or with shared peptides toggled, without paying for
peak-finding again.
}
\value{
An \code{mzlibr_median_polish}: \code{peptides_file}, \code{parameters}, \code{output_directory} (\code{NA}
  when nothing was written), \code{peptide_count} - distinct peptides read from the table - and
  \code{protein_count}, in protein groups; \code{samples}, a data.frame with one row per sample (\code{label},
  \code{condition}, \code{biological_replicate}, 0-based); and \code{proteins}, long, one row per protein group
  per sample, with \code{protein_group}, \code{gene_name}, \code{organism}, \code{sample} (a \code{samples$label}) and
  \code{intensity} in the instrument's intensity units.

}
\section{What 0 and NA mean}{


A protein \code{intensity} is \strong{NA} where median polish could not resolve a number - a degenerate
peptide matrix - and \strong{0} where the protein was not measured in that sample. They are
different facts, the same two \code{\link{flashlfq_quantify}} reports. A sample of several runs
(fractions, technical replicates) reports their sum.

}
\section{Sample labels}{


A sample is labelled by its run name when no design groups runs, and \code{"<condition>_<n>"} once
one does, where \code{n} is the biological replicate plus one: \code{control_1}. Runs are names, never
opened as files: nothing but the peptide table is read, so peak-level facts - match-between-runs
peaks, retention times - are not available here. Use \code{\link{flashlfq_quantify}} for those.

}
\section{Wraps}{
Wire verb \code{quant median-polish}. Generated from the bridge's verb spec \code{quant.median-polish.yaml} (bridge commit \code{5db922d4cfe1}) by \code{scripts/build-man.R}; the spec owns these facts, and all three bindings render the same ones.

\itemize{
\item \href{https://github.com/smith-chem-wisc/mzLib/blob/23c2490e/mzLib/FlashLFQ/FlashLFQResults.cs}{\code{FlashLfqResults.CalculateProteinResultsMedianPolish}} in \code{mzLib/FlashLFQ/FlashLFQResults.cs} at mzLib \code{23c2490e}
\item \href{https://github.com/smith-chem-wisc/mzLib/blob/23c2490e/mzLib/FlashLFQ/FlashLFQResults.cs}{\code{FlashLfqResults.WriteResults}} in \code{mzLib/FlashLFQ/FlashLFQResults.cs} at mzLib \code{23c2490e}
\item \href{https://github.com/smith-chem-wisc/mzLib/blob/23c2490e/mzLib/FlashLFQ/ProteinGroup.cs}{\code{ProteinGroup}} in \code{mzLib/FlashLFQ/ProteinGroup.cs} at mzLib \code{23c2490e}
\item \href{https://github.com/smith-chem-wisc/mzLib/blob/23c2490e/mzLib/MassSpectrometry/SpectraFileInfo.cs}{\code{SpectraFileInfo}} in \code{mzLib/MassSpectrometry/SpectraFileInfo.cs} at mzLib \code{23c2490e}
}
}
\section{Parameters: units, ranges and defaults}{
\describe{
\item{\code{peptides}}{path; required. A FlashLFQ QuantifiedPeptides.tsv. Columns are found by header name: Sequence, Base Sequence and Protein Groups are required; Gene Names (or Gene Name) and Organism are optional; one Intensity_<run> per run, each optionally with a Detection Type_<run>. A blank intensity cell is 0 (not measured).}
\item{\code{design} (wire \code{--stdin})}{string[]; default absent; range \code{none, or exactly one line per Intensity_ run}. The experimental design, one run per line, tab-separated: run_name[TAB condition[TAB biorep[TAB techrep[TAB fraction]]]], run_name matching an Intensity_<run_name> column (a name, never opened as a file). Omitted fields: condition "", biorep = the line's 0-based index, techrep 0, fraction 0. With no lines at all, every Intensity_ column is its own biological replicate with a blank condition, as FlashLFQ assumes with no design file. Median polish groups runs by condition and biological replicate, so this is how a caller says which runs are replicates of which sample.}
\item{\code{use_shared_peptides} (wire \code{--shared-peptides})}{flag; default \code{FALSE}. Let peptides shared between protein groups contribute (UseSharedPeptidesForProteinQuant). Off: a group with only shared peptides quantifies to 0.}
\item{\code{output_directory} (wire \code{--out})}{path; default absent. Directory (created if absent) where FlashLFQ also writes QuantifiedProteins.tsv. Absent or blank writes nothing.}
}
}
\section{Returned fields}{
Each field with its type, its unit, and what \code{NA} means when it is \code{NA}.

\describe{
\item{\code{peptides_file}}{string; never \code{NA}. Absolute path of the peptide table.}
\item{\code{parameters}}{object; never \code{NA}. \{use_shared_peptides_for_protein_quant\}.}
\item{\code{samples}}{object[]; never \code{NA}. One per (condition, biological replicate), ordered by condition (ordinal) then biorep, as the engine orders them; fields under result.tables.samples. Their labels are the keys of every protein's intensities.}
\item{\code{peptide_count}}{int; in \strong{peptides}; never \code{NA}. Distinct sequences read from the table (rows with a Sequence).}
\item{\code{protein_count}}{int; in \strong{protein groups}; never \code{NA}. Entries in proteins.}
\item{\code{proteins}}{object[]; never \code{NA}. One per protein group, ordered by protein_group (ordinal); fields under result.columns.}
\item{\code{output_directory}}{string; \code{NA} when out was not given (or was blank): nothing was written. Absolute path of out.}
}
}
\section{Columns of \code{proteins}}{
\describe{
\item{\code{protein_group}}{string; never \code{NA}. Protein group name; a ';'-joined Protein Groups cell is split into one group per name.}
\item{\code{gene_name}}{string; never \code{NA}. From the table's gene column, paired to the group by position when the counts match, else the whole ';'-joined cell; "" when the table has no gene column.}
\item{\code{organism}}{string; never \code{NA}. As gene_name, from the Organism column.}
\item{\code{intensity} (wire \code{intensities})}{map<string,float>; in \strong{intensity (instrument units)}; never \code{NA}. Sample label -> protein intensity. A value is null when median polish could not resolve one (NaN: a degenerate peptide matrix); 0 means not measured in that sample. A sample of several runs (fractions, technical replicates) reports their sum, as FlashLFQ's own output does.}
}
}
\section{Fields of \code{samples}}{
\describe{
\item{\code{label}}{string; never \code{NA}. The run name when no design groups runs (every condition blank or Default, and one fraction); otherwise '<condition>_<biorep + 1>', e.g. control_1.}
\item{\code{condition}}{string; never \code{NA}. The sample's condition; "" with no design.}
\item{\code{biological_replicate}}{int; never \code{NA}. 0-based (the label adds 1).}
}
}
\section{Errors}{
Each is an R condition carrying the class shown and \code{mzlib_error}; see \code{\link{mzlib_error}}.

\describe{
\item{\code{mzlib_usage_error} (usage)}{peptides missing or blank, or not found; the file empty; a required column (Sequence, Base Sequence, Protein Groups) missing; no Intensity_ columns; a sequence on two rows; an intensity cell that is not a number; a Detection Type_ cell that is not a FlashLFQ DetectionType name}
\item{\code{mzlib_usage_error} (usage)}{a design line with no run name, a run named twice, or a replicate/fraction that is not a non-negative integer; design runs with no Intensity_ column; Intensity_ runs the design does not mention (a design must name every run and only those)}
\item{\code{mzlib_bridge_error} (correctness)}{FlashLFQ's reconstruction or median polish fails}
\item{\code{mzlib_service_unavailable} (service_unavailable)}{Never raised by this verb.}
}
}
\section{Caveats}{
\itemize{
\item Detection types: where the table has no Detection Type_ column for a run (or the cell is blank), a positive intensity is treated as MSMS and 0 as NotDetected. That is this verb's only inference; a present but unrecognised value is an error, not a guess.
\item Protein intensity null means unquantifiable, 0 means not measured; neither is a measured zero.
\item Runs are names, not files: nothing but the peptide table is opened, and peak-level information (MBR peaks, retention times) is not available here. Use quant flashlfq for those.
\item With out, QuantifiedProteins.tsv labels its columns by the same rule as samples[].label at this pin (mzLib#1129 is in 23c2490e). Before #1129 the file's rule was inverted for unfractionated data; the values always agreed.
}
}
\section{Performance}{
Every call starts one bridge process, which costs a .NET start-up before any work. 
}
\section{Same verb in other bindings}{
\itemize{
\item Python (pyMzLib): \code{pymzlib.flashlfq.median_polish}
\item Rust (mzLibRust): \code{mzlib::flashlfq::median_polish_with} with \code{MedianPolishOptions}
\item R (mzLibR): \code{flashlfq_median_polish}
}
}
\section{Since}{
Wire protocol 1; pyMzLib 0.1.0; mzLibRust not yet shipped; mzLibR not yet shipped.
}
\section{Not yet verified}{
The spec records these as open. They are listed rather than hidden:

\itemize{
\item Not yet in mzLibRust or mzLibR. Intended names mirror each binding's flashlfq module: Rust mzlib::flashlfq::median_polish (defaults) and median_polish_with + MedianPolishOptions \{design, use_shared_peptides, output_directory, timeout\} beside quantify_with + QuantifyOptions (the DesignEntry builder in the example is a sketch); R flashlfq_median_polish(peptides, design = NULL, use_shared_peptides = FALSE, output_directory = NULL, timeout = NULL) beside flashlfq_quantify, design as a data frame. The checker cannot verify them.
\item The bridge's own comment in MedianPolishToWire and pyMzLib's median_polish docstring still say the TSV labels disagree until mzLib#1129 lands; it is in the pin, so both are stale. The agreement stated in the caveat is from reading ProteinGroup.cs at 23c2490e, not from a recorded \code{--out} run.
\item median_polish_small.json is hand-shaped ('/abs/...'), not a live recording.
\item The design's techrep and fraction are parsed but only fraction feeds the label rule; how FlashLFQ's median polish uses techrep is mzLib's and was not traced.
\item The wire params peptides/shared-peptides/out project as peptides/use_shared_peptides/output_directory in pyMzLib; the spec lint will need PYTHON_DEVIATIONS entries (stdin -> design, shared-peptides -> use_shared_peptides, out -> output_directory).
}
}
\references{
\itemize{
\item \href{https://doi.org/10.1021/acs.jproteome.7b00608}{doi:10.1021/acs.jproteome.7b00608}: FlashLFQ, whose protein quantification this verb reruns (Millikin et al., J Proteome Res 2018)
}
}
\seealso{
\code{\link{flashlfq_quantify}}
}
\examples{

\dontshow{.mzlibr_example <- mzLibR:::replay_bridge_start()}
polished <- flashlfq_median_polish("QuantifiedPeptides.tsv",
  design = data.frame(file_name = c("run_3", "run_4"), condition = c("control", "treated"),
                      biological_replicate = 0))
polished
polished$samples
polished$proteins
\dontshow{mzLibR:::replay_bridge_stop(.mzlibr_example)}
}
