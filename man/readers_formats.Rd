% Generated by scripts/build-man.R from the roxygen-style block in the source.
% Do not edit by hand; edit R/ and re-run.
\name{readers_formats}
\alias{readers_formats}
\title{Every file type mzLib can recognise}
\usage{
readers_formats(timeout = 60)
}
\arguments{
\item{timeout}{Seconds to allow, or \code{NULL} to wait indefinitely.
}

}
\description{
Enumerated from mzLib itself rather than from a list maintained here, so it reflects the
installed version and cannot go stale.
}
\value{
A data.frame with one row per format: \code{file_type}, \code{extension}, \code{reader},
  \code{is_quantifiable}, and a \code{views} list column.

}
\section{Views, and why most formats have none}{


It is tempting to read "29 formats" as "29 formats in one uniform shape". They are not. The
formats fall into disjoint families, and \strong{13 of the 29 belong to none of them} — an empty
\code{views} is a real and common answer, meaning mzLib can parse the file but offers no
cross-format projection of it.

- \code{"quantifiable"} — the cross-format record view, and the input \code{\link{flashlfq_quantify}}
  accepts. \strong{Exactly three file types have it}: MetaMorpheus \code{psmtsv} and \code{osmtsv}, and
  \code{MsFraggerPsm}.
- \code{"ms1_features"} — deconvolved MS1 features (TopFD \code{_ms1.feature}, Dinosaur).
- \code{"spectra"} — the file is spectra, not results.
- \code{"spectral_match"} — identifications that share no file-level interface.

Note also that \code{extension} is \strong{not unique}: \code{BrukerD} and \code{BrukerTimsTof} are both \code{.d},
told apart by what the directory holds, and several formats share \code{.tsv}.

}
\seealso{
\code{\link{readers_identify}}
}
