% Generated by scripts/build-man.R from the roxygen-style block in the source.
% Do not edit by hand; edit R/ and re-run.
\name{sdrf_all}
\alias{sdrf_all}
\title{Every cell under a column, one list element per row}
\usage{
sdrf_all(doc, column)
}
\arguments{
\item{doc}{An \code{\link{sdrf_read}} or \code{\link{sdrf_pool}} result.}

\item{column}{A column name.
}

}
\description{
The accessor for a multi-cardinality column such as \code{comment[modification parameters]}, which
legitimately repeats - up to eight times in one corpus file. Positions a row is too short to
reach are skipped, so each element holds only cells that exist.
}
\value{
A list with one character vector per returned row.
}
\examples{

\dontshow{.mzlibr_example <- mzLibR:::replay_bridge_start()}
doc <- sdrf_read("PXD000070.sdrf.tsv")
sdrf_all(doc, "comment[modification parameters]")[[1]]
\dontshow{mzLibR:::replay_bridge_stop(.mzlibr_example)}
}
