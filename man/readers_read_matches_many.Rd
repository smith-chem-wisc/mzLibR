% Generated by scripts/build-man.R from the roxygen-style block in the source.
% Do not edit by hand; edit R/ and re-run.
\name{readers_read_matches_many}
\alias{readers_read_matches_many}
\title{Read identifications from many files, in one bridge call}
\usage{
readers_read_matches_many(paths, scores = FALSE, out = NULL, threads = 1,
  on_error = "fail", timeout = NULL)
}
\arguments{
\item{paths}{A character vector of paths to MsPathFinderT, Casanovo or mzIdentML files.}

\item{scores}{Make the table long by score - one row per match and engine score - as
  \code{\link{readers_read_matches}} does.}

\item{out}{Write the long table here as tab-separated text and return only a summary.}

\item{threads}{Files read at once. \code{1}, the default, or \code{-1} for one per core. The table does
  not depend on it.}

\item{on_error}{\code{"fail"}, the default, or \code{"skip"} to record an unreadable file in \code{files} and
  read the rest.}

\item{timeout}{Seconds to allow, or \code{NULL} to wait indefinitely.
}

}
\description{
Read identifications through the cross-format spectral_match view - scan, sequences, accession, decoy flag, modifications and the q-value, rank and threshold a format records - optionally with each engine's scores as long rows.

The many-files form of \code{\link{readers_read_matches}}.
}
\value{
An \code{mzlibr_read_batch}. \code{records} begins with \code{source_index} (1-based, into \code{paths}) and
  \code{source_path}, then the columns of \code{\link{readers_read_matches}}: \code{q_value} a fraction from 0 to 1,
  and with \code{scores = TRUE} \code{score_value} in the engine's own units, named by \code{score_name}.
  \code{files} has one row per input, with \code{skipped_count} in items for mzIdentML. \code{file_count},
  \code{read_count} and \code{failed_count} count files; \code{record_count} and \code{returned_count} count
  matches; \code{row_count} counts rows, more than matches with \code{scores = TRUE}.

}
\section{Wraps}{
Wire verb \code{readers read-matches} with \code{--paths-stdin}. Generated from the bridge's verb spec \code{readers.read-matches.yaml} (bridge commit \code{5db922d4cfe1}) by \code{scripts/build-man.R}; the spec owns these facts, and all three bindings render the same ones.

\itemize{
\item \href{https://github.com/smith-chem-wisc/mzLib/blob/23c2490e/mzLib/Readers/BaseClasses/ISpectralMatch.cs}{\code{ISpectralMatch}} in \code{mzLib/Readers/BaseClasses/ISpectralMatch.cs} at mzLib \code{23c2490e}
\item \href{https://github.com/smith-chem-wisc/mzLib/blob/23c2490e/mzLib/Readers/ExternalResults/ResultFiles/MzIdentMLResultFile.cs}{\code{MzIdentMLResultFile.SkippedMatches}} in \code{mzLib/Readers/ExternalResults/ResultFiles/MzIdentMLResultFile.cs} at mzLib \code{23c2490e}
\item \href{https://github.com/smith-chem-wisc/mzLib/blob/23c2490e/mzLib/Readers/ExternalResults/IndividualResultRecords/MzIdentMLRecord.cs}{\code{MzIdentMLRecord.Scores}} in \code{mzLib/Readers/ExternalResults/IndividualResultRecords/MzIdentMLRecord.cs} at mzLib \code{23c2490e}
}
}
\section{Parameters: units, ranges and defaults}{
\describe{
\item{\code{scores}}{flag; default \code{FALSE}. Make the table long by score: one row per match and engine score, adding match_index, score_name and score_value. Only mzIdentML records scores (#1306); other formats keep one row per match with the two columns in absent_fields. offset and limit still count matches.}
\item{\code{paths} (wire \code{--paths-stdin})}{flag; default absent. Read MANY files: newline-delimited paths on stdin (blank lines ignored; a repeated path is a usage error). Exclusive with path; offset and limit are refused with it.}
\item{\code{threads}}{int; in \strong{files}; default \code{1}; range \code{>= 1, or -1 for one per core}. Files read at once. The output is byte-identical at any value (files are emitted in input order); 1 is the default because each file in flight is held whole in memory.}
\item{\code{on_error} (wire \code{--on-error})}{string; default \code{fail}; range \code{fail | skip}. fail: the first unreadable input stops the batch with its usage or correctness error, the message starting 'Input <i> (<path>)'. skip: the failure goes in that input's files[i].error and the rest are read. skip with a single path is a usage error.}
\item{\code{out}}{path; default absent. Write the long table here, one file at a time in input order, so memory holds at most threads files. A batch that stops on an error deletes its partial table. Must differ from every input.}
}
}
\section{Returned fields}{
Each field with its type, its unit, and what \code{NA} means when it is \code{NA}.

\describe{
\item{\code{file_count}}{int; in \strong{files}; never \code{NA}. Paths given.}
\item{\code{read_count}}{int; in \strong{files}; never \code{NA}. Files read.}
\item{\code{failed_count}}{int; in \strong{files}; never \code{NA}. Files not read; non-zero only under on-error skip.}
\item{\code{record_count}}{int; in \strong{matches}; never \code{NA}. Matches summed over the files read (BULK.md section 2).}
\item{\code{returned_count}}{int; in \strong{matches}; never \code{NA}. Matches returned in columns, summed over the files; 0 when written to out.}
\item{\code{row_count}}{int; in \strong{rows}; never \code{NA}. Rows in columns (more than returned_count for a long view); 0 when written to out.}
\item{\code{on_error}}{string; never \code{NA}. fail or skip, as requested. \code{--threads} is deliberately not echoed: the envelope is byte-identical at every thread count.}
\item{\code{scores_included}}{bool; never \code{NA}. Whether \code{--scores} was given.}
\item{\code{column_names}}{string[]; never \code{NA}. source_index, source_path, then the single-file columns.}
\item{\code{records} (wire \code{columns})}{table; \code{NA} when the table was written to out instead. One long table: rows grouped by input in input order, each file's rows in its own order, whatever \code{--threads} is.}
\item{\code{output}}{object; \code{NA} when out was not given. \{path, format: "tsv", row_count\}; written one file at a time.}
\item{\code{files}}{object[]; never \code{NA}. One per input, in input order: the per-file block (path, file_type, reader, skipped_count, skipped, record_count, rows_not_read, retention_time_unit, caveats, column_names, absent_fields, failed_fields, excluded_fields, error). An unread input has error set and every fact it could not establish null or empty.}
}
}
\section{Columns of \code{records}}{
The first two columns say which input each row came from - \code{source_index} is 1-based in R, so it indexes \code{files} directly - and rows are grouped by input, in input order, whatever \code{threads} is.
\describe{
\item{\code{source_index}}{int; never \code{NA}. 0-based position of the row's input in the stdin list.}
\item{\code{source_path}}{string; never \code{NA}. Absolute path of the row's input.}
\item{\code{file_name_without_extension}}{string; never \code{NA}. The spectra file of the match.}
\item{\code{one_based_scan_number}}{int; never \code{NA}. Scan number; see caveats for mzIdentML index= and Casanovo.}
\item{\code{base_sequence}}{string; never \code{NA}. Unmodified sequence.}
\item{\code{full_sequence}}{string; never \code{NA}. Modified sequence.}
\item{\code{accession}}{string; \code{NA} when the property threw (then in failed_fields). Protein accession(s).}
\item{\code{is_decoy}}{bool; \code{NA} when every format but MsPathFinderT (then in absent_fields). Decoy flag.}
\item{\code{modifications}}{string; never \code{NA}. 'position:name' pairs, ';'-joined, 1 = N-terminus.}
\item{\code{modification_count}}{int; never \code{NA}. Modifications on the match.}
\item{\code{q_value}}{float; in \strong{fraction (0 to 1)}; \code{NA} when the format has no q-value (in absent_fields) or this mzIdentML item reports none. PSM-level q-value (mzIdentML MS:1002354 or a child; MsPathFinderT QValue).}
\item{\code{rank}}{int; \code{NA} when not mzIdentML (in absent_fields). The item's rank; filter on rank == 1.}
\item{\code{pass_threshold}}{bool; \code{NA} when not mzIdentML (in absent_fields). The item's passThreshold.}
\item{\code{match_index}}{int; never \code{NA}. 0-based position of the match among the file's matches. \emph{Present only with \code{scores}.}}
\item{\code{score_name}}{string; \code{NA} when the match has no scores, or the format records none (in absent_fields). Engine score name as written, e.g. MS-GF:SpecEValue. \emph{Present only with \code{scores}.}}
\item{\code{score_value}}{float; in \strong{engine-defined (see score_name)}; \code{NA} when as score_name. The score's value. \emph{Present only with \code{scores}.}}
}
}
\section{Errors}{
Each is an R condition carrying the class shown and \code{mzlib_error}; see \code{\link{mzlib_error}}.

\describe{
\item{\code{mzlib_usage_error} (usage)}{path missing or blank; file or directory not found; mzLib does not recognise the file type (the message points at readers formats)}
\item{\code{mzlib_usage_error} (usage)}{offset, limit or out given with no value; offset or limit < 0; non-integer value; out resolves to the same file as path}
\item{\code{mzlib_usage_error} (usage)}{the file has no spectral_match view}
\item{\code{mzlib_bridge_error} (correctness)}{mzLib recognises the file and fails to parse it}
\item{\code{mzlib_usage_error} (usage)}{paths-stdin with path, offset or limit; paths-stdin with a value; no paths on stdin; a repeated path; threads 0 or below -1; on-error not fail or skip; on-error skip with a single path; out naming an input}
\item{\code{mzlib_usage_error} (usage)}{with on-error fail, an input that is missing or not this view: the usage error of that input, prefixed 'Input <i> (<path>)'}
\item{\code{mzlib_bridge_error} (correctness)}{with on-error fail, mzLib failing to parse an input: that input's own error type, message prefixed 'Input <i> (<path>)'}
\item{\code{mzlib_service_unavailable} (service_unavailable)}{Never raised by this verb.}
}
}
\section{Caveats}{
\itemize{
\item Nothing is FDR-filtered. q_value is filled only by mzIdentML and an MsPathFinderT file with a QValue column; an _IcTarget/_IcDecoy file has none and names it absent (mzLib would report 0).
\item mzIdentML: every SpectrumIdentificationItem is a row, lower ranks and failed thresholds included (MzIdentMLResultFile.cs:177). Filter on rank == 1 and pass_threshold.
\item mzIdentML: is_decoy is absent because the isDecoy attribute defaults to false when omitted (MzIdentMLResultFile.cs:173).
\item mzIdentML: items mzLib cannot represent are skipped, not failed, and listed in skipped (MzIdentMLResultFile.cs:123).
\item MsPathFinderT: is_decoy comes from an XXX protein-name prefix (MsPathFinderTResult.cs:92); accession assumes a UniProt header and lands in failed_fields otherwise.
\item Casanovo: one_based_scan_number is the mzTab index plus one (CasanovoMzTabFile.cs:116).
\item Many files: \code{--paths-stdin} reads the list in ONE process, \code{--threads} at a time, into one long table with source_index and source_path first. The output does not depend on \code{--threads} (a tested property); the default of 1 is a memory choice, since every reader materialises the whole file.
}
}
\section{Performance}{
Every call starts one bridge process, which costs a .NET start-up before any work. This bulk form pays it once for the whole list, which is the reason it exists: never loop the one-path function over many files.
}
\section{Same verb in other bindings}{
\itemize{
\item Python (pyMzLib): \code{pymzlib.readers.read_matches}; many files: \code{pymzlib.readers.read_matches_many}
\item Rust (mzLibRust): \code{mzlib::readers::read_matches_with} with \code{MatchOptions}; many files: \code{mzlib::readers::read_matches_many}
\item R (mzLibR): \code{readers_read_matches}; many files: \code{\link{readers_read_matches_many}}
}
}
\section{Since}{
Wire protocol 1; pyMzLib 0.1.0; mzLibRust 0.1.0; mzLibR 0.1.0.
}
\section{Not yet verified}{
The spec records these as open. They are listed rather than hidden:

\itemize{
\item The _many spellings (py, rust, r) follow the 2026-09-23 cross-binding decision; Rust and R names here are intended, not yet ported, and since.pymzlib stays null until release.
\item readers_matches_mzid_scores.json (\code{--scores}) is replayed through the pyMzLib conftest rather than listed here: check_verbs.py excludes present_when columns from its column check, so a \code{--scores} fixture cannot be a spec example yet.
\item q_value, rank and pass_threshold join a view whose other columns are Readers.ISpectralMatch members; they are read from the record types that carry them. If mzLib adds them to the interface, the per-format reads go away.
}
}
\seealso{
\code{\link{readers_read_matches}}
}
\examples{

\dontrun{
# Your own files: no recording of a many-file read of this view exists to replay.
batch <- readers_read_matches_many(c("a.mzid", "b.mzid"), threads = 2)
batch$records[batch$records$pass_threshold %in% TRUE, ]
}
}
