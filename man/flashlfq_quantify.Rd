% Generated by scripts/build-man.R from the roxygen-style block in the source.
% Do not edit by hand; edit R/ and re-run.
\name{flashlfq_quantify}
\alias{flashlfq_quantify}
\title{Quantify a search's peptides across mzML runs with FlashLFQ}
\usage{
flashlfq_quantify(psms, spectra, normalize = FALSE, ppm_tolerance = 10,
  isotope_ppm_tolerance = 5, integrate = FALSE, match_between_runs = FALSE,
  mbr_ppm_tolerance = 10, mbr_q_value_threshold = 0.05,
  use_shared_peptides_for_protein_quant = FALSE, bayesian_protein_quant = FALSE,
  use_pep_q_value = FALSE, max_threads = 1, output_directory = NULL, timeout = NULL)
}
\arguments{
\item{psms}{Path to a PSM result file. \strong{Use a MetaMorpheus \code{.psmtsv} or \code{.osmtsv}.} Every
  run named in the file must have a matching mzML in \code{spectra}; FlashLFQ matches
  identifications to runs by base file name.}

\item{spectra}{The mzML runs. Either a character vector of paths, or a data.frame with a
  \code{path} column and optional \code{condition}, \code{biological_replicate}, \code{technical_replicate} and
  \code{fraction} columns. Omitted design fields get the defaults MetaMorpheus uses with no
  experimental-design file.

  \strong{mzML only.} \code{.raw} and \code{.d} are rejected up front; convert them first.}

\item{normalize}{Whether to normalise between runs.}

\item{ppm_tolerance}{Mass tolerance for peak-finding, in ppm.}

\item{isotope_ppm_tolerance}{Mass tolerance for isotope-envelope matching, in ppm.}

\item{integrate}{Whether to integrate peak areas rather than take apex intensity.}

\item{match_between_runs}{Whether to transfer identifications between runs.

  MBR needs a \strong{complete, balanced design} to work properly, and \code{mbr_q_value_threshold} is
  its FDR control - without it roughly 80\% of transfers are false. Set \code{condition} and
  \code{biological_replicate} on \code{spectra} so FlashLFQ knows which runs are comparable.

  \strong{Whatever you do, count transfers from \code{peaks}, never from \code{peptides}.} See the return
  value.}

\item{mbr_ppm_tolerance}{Mass tolerance for match-between-runs, in ppm.}

\item{mbr_q_value_threshold}{FDR threshold for accepting a transfer.}

\item{use_shared_peptides_for_protein_quant}{Whether peptides shared between protein groups
  contribute to protein quantification.

  \code{FALSE} is the default and it is the main reason protein intensities come back as \strong{0}: on
  mzLib's K562 pair, \strong{847} of 943 protein groups are 0 in both runs, mostly because their
  only evidence is shared peptides.}

\item{bayesian_protein_quant}{Whether to use the Bayesian protein quantification model.}

\item{use_pep_q_value}{Whether to use PEP q-values rather than q-values for filtering.}

\item{max_threads}{Worker threads. \strong{Defaults to 1 here, which differs from pyMzLib's -1, and
  deliberately.}

  With more than one thread the peptide roll-up nondeterministically drops MBR intensities,
  so identical inputs give different protein-level answers roughly \strong{1 run in 6} - a
  borderline protein was unresolvable in 5 of 6 repeats. Any figure produced multithreaded
  may not reproduce (smith-chem-wisc/mzLib#1111). mzLibR warns if you set anything else.}

\item{output_directory}{Where FlashLFQ should write its TSV output, or \code{NULL} to write none.}

\item{timeout}{Seconds to allow, or \code{NULL} to wait as long as it takes.
}

}
\description{
Quantify a search's peptides across mzML runs with FlashLFQ
}
\value{
An \code{mzlibr_quant}: \code{psm_file}, \code{identification_count}, \code{parameters},
  \code{output_directory}, and four tidy data.frames - \code{spectra_files}, \code{peptides}, \code{proteins}
  and \code{peaks}.

}
\section{Read peaks, not the peptide roll-up}{


\code{peptides} is FlashLFQ's roll-up and \strong{it drops most match-between-runs transfers.} On
mzLib's own K562 pair there are \strong{140} true transfers in \code{peaks} and the peptide table shows
\strong{52} - a 63\% under-count. Worse, it is not evenly spread: per run the peaks give run_3
\strong{62} and run_4 \strong{78}, while the roll-up gives run_3 \strong{0} and run_4 52. Read only the
roll-up and MBR appears not to have worked at all in half the experiment.

"Peptides quantified in both runs" is \strong{257} from \code{peaks} and \strong{169} from \code{peptides}.

Nor can you reproduce the roll-up by pivoting \code{peaks} yourself: where a run has several peaks
for one peptide the roll-up reports one rather than their sum.

}
\section{\code{proteins$file_name} is a sample, not a file}{


FlashLFQ measures peptides in \strong{files} but resolves proteins across \strong{samples}, grouping runs
by condition and biological replicate before the median-polish roll-up. \code{flashlfq_quantify()}
gives every run its own sample, so \code{proteins$file_name} and \code{peptides$file_name} carry the same
run base names and the two frames join cleanly on it.

The distinction only bites if runs are ever grouped into replicates: \code{proteins$file_name} would
then hold a sample label (\code{"condition_replicate"}) covering several runs, while
\code{peptides$file_name} stayed per run. The column name is kept for symmetry between the two
frames; read it as "the thing this intensity was measured over".

}
\section{What 0 and NA mean, and which is rare}{


\code{peptides$intensity} is \strong{0} when the peptide was not measured in that run. It is never NA.

\code{proteins$intensity} is \strong{NA} when FlashLFQ could not resolve a number at all - its
median-polish produced NaN. Arithmetic propagates it, so \code{mean()} on a protein column returns
NA rather than a confidently wrong number, and \code{na.rm = TRUE} is a choice you make visibly.
mzLibR never applies it on your behalf.

\strong{NA is the rare outcome and 0 is the common one.} On the K562 pair, \strong{2} proteins are NA
and \strong{847} are 0 in both runs. "No usable number" is 849; "could not be resolved" is 2.

}
\seealso{
\code{\link{flashlfq_mbr_peaks}}, \code{\link{flashlfq_mbr_rescued_peptide_count}}
}
