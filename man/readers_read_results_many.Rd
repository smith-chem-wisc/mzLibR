% Generated by scripts/build-man.R from the roxygen-style block in the source.
% Do not edit by hand; edit R/ and re-run.
\name{readers_read_results_many}
\alias{readers_read_results_many}
\title{Read many result files into the uniform record view, in one bridge call}
\usage{
readers_read_results_many(paths, out = NULL, threads = 1, on_error = "fail",
  timeout = NULL)
}
\arguments{
\item{paths}{A character vector of paths to files offering the \code{"quantifiable"} view.}

\item{out}{Write the long table here as tab-separated text, one file at a time, and return only
  a summary.}

\item{threads}{Files read at once. \code{1}, the default, holds one whole file in memory at a time;
  \code{-1} uses one per core. The table does not depend on it.}

\item{on_error}{\code{"fail"}, the default, stops at the first file that cannot be read. \code{"skip"}
  records the failure in that file's row of \code{files} and reads the rest.}

\item{timeout}{Seconds to allow, or \code{NULL} to wait indefinitely.
}

}
\description{
Read a result file through the cross-format quantifiable view: sequence, retention time, charge, theoretical mass, decoy flag and protein groups, the same columns for every format that offers it.

The many-files form of \code{\link{readers_read_results}}.
}
\value{
An \code{mzlibr_read_batch}. \code{records} begins with \code{source_index} (1-based, into \code{paths}) and
  \code{source_path}, then the columns of \code{\link{readers_read_results}}: \code{retention_time} in each file's
  own \code{retention_time_unit} - minutes for all four formats at mzLib 1.0.592 - \code{charge_state} a
  charge, \code{monoisotopic_mass} in Da. Convert times with \code{\link{readers_retention_time_in_minutes}},
  which applies each file's unit to its rows. \code{files} has one row per input: \code{record_count} in
  records, \code{rows_not_read} in rows, \code{retention_time_unit}, \code{caveats}, \code{absent_fields} and the
  error columns. \code{file_count}, \code{read_count} and \code{failed_count} count files; \code{record_count} and
  \code{returned_count} count records over the files read; \code{row_count} counts the rows of \code{records}.

}
\section{Wraps}{
Wire verb \code{readers read-results} with \code{--paths-stdin}. Generated from the bridge's verb spec \code{readers.read-results.yaml} (bridge commit \code{5db922d4cfe1}) by \code{scripts/build-man.R}; the spec owns these facts, and all three bindings render the same ones.

\itemize{
\item \href{https://github.com/smith-chem-wisc/mzLib/blob/23c2490e/mzLib/Readers/FileReader.cs}{\code{FileReader.ReadQuantifiableResultFile}} in \code{mzLib/Readers/FileReader.cs} at mzLib \code{23c2490e}
\item \href{https://github.com/smith-chem-wisc/mzLib/blob/23c2490e/mzLib/Readers/BaseClasses/IQuantifiableRecord.cs}{\code{IQuantifiableRecord}} in \code{mzLib/Readers/BaseClasses/IQuantifiableRecord.cs} at mzLib \code{23c2490e}
}
}
\section{Parameters: units, ranges and defaults}{
\describe{
\item{\code{paths} (wire \code{--paths-stdin})}{flag; default absent. Read MANY files: newline-delimited paths on stdin (blank lines ignored; a repeated path is a usage error). Exclusive with path; offset and limit are refused with it.}
\item{\code{threads}}{int; in \strong{files}; default \code{1}; range \code{>= 1, or -1 for one per core}. Files read at once. The output is byte-identical at any value (files are emitted in input order); 1 is the default because each file in flight is held whole in memory.}
\item{\code{on_error} (wire \code{--on-error})}{string; default \code{fail}; range \code{fail | skip}. fail: the first unreadable input stops the batch with its usage or correctness error, the message starting 'Input <i> (<path>)'. skip: the failure goes in that input's files[i].error and the rest are read. skip with a single path is a usage error.}
\item{\code{out}}{path; default absent. Write the long table here, one file at a time in input order, so memory holds at most threads files. A batch that stops on an error deletes its partial table. Must differ from every input.}
}
}
\section{Returned fields}{
Each field with its type, its unit, and what \code{NA} means when it is \code{NA}.

\describe{
\item{\code{file_count}}{int; in \strong{files}; never \code{NA}. Paths given.}
\item{\code{read_count}}{int; in \strong{files}; never \code{NA}. Files read.}
\item{\code{failed_count}}{int; in \strong{files}; never \code{NA}. Files not read; non-zero only under on-error skip.}
\item{\code{record_count}}{int; in \strong{records}; never \code{NA}. Records summed over the files read (BULK.md section 2).}
\item{\code{returned_count}}{int; in \strong{records}; never \code{NA}. Records returned in columns, summed over the files; 0 when written to out.}
\item{\code{row_count}}{int; in \strong{rows}; never \code{NA}. Rows in columns (more than returned_count for a long view); 0 when written to out.}
\item{\code{on_error}}{string; never \code{NA}. fail or skip, as requested. \code{--threads} is deliberately not echoed: the envelope is byte-identical at every thread count.}
\item{\code{column_names}}{string[]; never \code{NA}. source_index, source_path, then the single-file columns.}
\item{\code{records} (wire \code{columns})}{table; \code{NA} when the table was written to out instead. One long table: rows grouped by input in input order, each file's rows in its own order, whatever \code{--threads} is.}
\item{\code{output}}{object; \code{NA} when out was not given. \{path, format: "tsv", row_count\}; written one file at a time.}
\item{\code{files}}{object[]; never \code{NA}. One per input, in input order: the per-file block (path, file_type, reader, record_count, rows_not_read, retention_time_unit, caveats, column_names, absent_fields, failed_fields, excluded_fields, error). An unread input has error set and every fact it could not establish null or empty.}
}
}
\section{Columns of \code{records}}{
The first two columns say which input each row came from - \code{source_index} is 1-based in R, so it indexes \code{files} directly - and rows are grouped by input, in input order, whatever \code{threads} is.
\describe{
\item{\code{source_index}}{int; never \code{NA}. 0-based position of the row's input in the stdin list.}
\item{\code{source_path}}{string; never \code{NA}. Absolute path of the row's input.}
\item{\code{file_name}}{string; never \code{NA}. The spectra file the record came from, as the format writes it (not a join key across formats).}
\item{\code{base_sequence}}{string; never \code{NA}. Unmodified sequence.}
\item{\code{full_sequence}}{string; never \code{NA}. Modified sequence; on an ambiguous psmtsv row every candidate '|'-joined.}
\item{\code{retention_time}}{float; in \strong{min}; \code{NA} when mzLib's -1 'absent' sentinel, or a non-finite value. Retention time, in retention_time_unit.}
\item{\code{charge_state}}{int; in \strong{charge}; never \code{NA}. Precursor charge.}
\item{\code{monoisotopic_mass}}{float; in \strong{Da}; \code{NA} when mzLib's -1 'absent' sentinel. THEORETICAL peptide mass, not the observed precursor.}
\item{\code{is_decoy}}{bool; \code{NA} when the format cannot report decoys: MSFragger and DIA-NN, where is_decoy is also in absent_fields. Decoy flag where the format carries one.}
\item{\code{protein_accessions}}{string; never \code{NA}. ';'-joined accessions.}
\item{\code{gene_name}}{string; never \code{NA}. ';'-joined gene names.}
\item{\code{organism}}{string; never \code{NA}. ';'-joined organisms.}
}
}
\section{Errors}{
Each is an R condition carrying the class shown and \code{mzlib_error}; see \code{\link{mzlib_error}}.

\describe{
\item{\code{mzlib_usage_error} (usage)}{path missing or blank; file or directory not found; mzLib does not recognise the file type (the message points at readers formats)}
\item{\code{mzlib_usage_error} (usage)}{offset, limit or out given with no value; offset or limit < 0; non-integer value; out resolves to the same file as path}
\item{\code{mzlib_usage_error} (usage)}{the file has no quantifiable view (the message names the views it does have)}
\item{\code{mzlib_bridge_error} (correctness)}{mzLib recognises the file and fails to parse it}
\item{\code{mzlib_usage_error} (usage)}{paths-stdin with path, offset or limit; paths-stdin with a value; no paths on stdin; a repeated path; threads 0 or below -1; on-error not fail or skip; on-error skip with a single path; out naming an input}
\item{\code{mzlib_usage_error} (usage)}{with on-error fail, an input that is missing or not this view: the usage error of that input, prefixed 'Input <i> (<path>)'}
\item{\code{mzlib_bridge_error} (correctness)}{with on-error fail, mzLib failing to parse an input: that input's own error type, message prefixed 'Input <i> (<path>)'}
\item{\code{mzlib_service_unavailable} (service_unavailable)}{Never raised by this verb.}
}
}
\section{Caveats}{
\itemize{
\item No q-value, PEP or score: nothing here is FDR-filtered.
\item is_decoy is absent for MSFragger and DIA-NN, which write no target/decoy label; null there means unknown, not target.
\item monoisotopic_mass is the theoretical mass in every format; the observed precursor is not in this view.
\item psmtsv: monoisotopic_mass keeps the first candidate of an ambiguous row while full_sequence keeps them all (SpectrumMatchFromTsv.cs:119, :194).
\item A malformed psmtsv row is dropped silently by mzLib; rows_not_read counts them.
\item Many files: \code{--paths-stdin} reads the list in ONE process, \code{--threads} at a time, into one long table with source_index and source_path first. The output does not depend on \code{--threads} (a tested property); the default of 1 is a memory choice, since every reader materialises the whole file.
}
}
\section{Performance}{
Every call starts one bridge process, which costs a .NET start-up before any work. This bulk form pays it once for the whole list, which is the reason it exists: never loop the one-path function over many files.
}
\section{Same verb in other bindings}{
\itemize{
\item Python (pyMzLib): \code{pymzlib.readers.read_results}; many files: \code{pymzlib.readers.read_results_many}
\item Rust (mzLibRust): \code{mzlib::readers::read_results_with} with \code{ReadOptions}; many files: \code{mzlib::readers::read_results_many}
\item R (mzLibR): \code{readers_read_results}; many files: \code{\link{readers_read_results_many}}
}
}
\section{Since}{
Wire protocol 1; pyMzLib 0.1.0; mzLibRust 0.1.0; mzLibR 0.1.0.
}
\section{Not yet verified}{
The spec records these as open. They are listed rather than hidden:

\itemize{
\item The _many spellings (py, rust, r) follow the 2026-09-23 cross-binding decision; Rust and R names here are intended, not yet ported, and since.pymzlib stays null until release.
}
}
\seealso{
\code{\link{readers_read_results}}
}
\examples{

\dontrun{
# Your own files: no recording of a many-file read of this view exists to replay.
batch <- readers_read_results_many(c("run_1/AllPSMs.psmtsv", "run_2/AllPSMs.psmtsv"),
  threads = 2)
table(batch$records$source_path)
}
}
