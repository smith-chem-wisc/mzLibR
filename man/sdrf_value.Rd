% Generated by scripts/build-man.R from the roxygen-style block in the source.
% Do not edit by hand; edit R/ and re-run.
\name{sdrf_value}
\alias{sdrf_value}
\title{The first cell under a column, one per row}
\usage{
sdrf_value(doc, column)
}
\arguments{
\item{doc}{An \code{\link{sdrf_read}} or \code{\link{sdrf_pool}} result.}

\item{column}{A column name.
}

}
\description{
\code{NA} means \strong{the document does not have this column, or the row is too short to reach it}. It
does not mean "empty": the SDRF reserved words \code{"not available"} and \code{"not applicable"} are real
values an experiment chose to write, and they come back as themselves.
}
\value{
A character vector with one element per returned row.
}
\seealso{
\code{\link{sdrf_all}} for a column that repeats.
}
