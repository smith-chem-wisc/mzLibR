# Print a batch read from many files

Print a batch read from many files

## Usage

``` r
# S3 method for class 'mzlibr_batch'
print(x, ...)
```

## Arguments

- x:

  A `_many` result, such as
  [`readers_read_spectra_many`](https://smith-chem-wisc.github.io/mzLibR/reference/readers_read_spectra_many.md).

- ...:

  Ignored.

## Value

`x`, invisibly.
